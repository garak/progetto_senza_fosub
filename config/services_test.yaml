services:
    _defaults:
        public: true
        autowire: true

    Fixtures\:
        resource: '../fixtures/*'
        tags: ['doctrine.fixture.orm']

    test.userprovider:
        alias: security.user.provider.concrete.progetto

