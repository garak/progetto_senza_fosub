{% extends 'layout.html.twig' %}

{% block title %}Homepage - {{ parent() }}{% endblock %}

{% block body %}
    <h2>Homepage</h2>
    <p>
        It could work!
    </p>
    <p>
        Utente: {{ app.user }} <a href="{{ path('cambia_password') }}" class="btn btn-info">cambia password</a>
    </p>
    <p>
        <a href="{{ path('logout') }}" class="btn btn-primary">logout</a>
    </p>
{% endblock %}
